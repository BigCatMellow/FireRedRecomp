# Task: add Local Worker Bridge route for Phase 4 player forced replacement

- Status: `ACTIVE`
- Type: `INFRASTRUCTURE / EXECUTION SUBSTRATE`

Add one exact fail-closed route for `phase4-player-forced-replacement-ui` before
implementation. Its literal later-worker surface is `main.lua`,
`src/core/BattleSceneController.lua`, two exact focused tests, one exact runtime
replay script, and task/coordination records. Preserve all existing bridge,
trusted-main, ROM SHA, test-order, no-wildcard, and independent-review gates;
add a separate one-file non-gameplay probe before Worker eligibility.
